    // <--[property]
    // @object ItemTag
    // @name equippable
    // @input MapTag
    // @plugin DenizenAdditions
    // @description
    // Controls how an item can be equipped by entities. <@link language Item Components>.
    // Map keys are "slot" (required, equipment slot name), "equip_sound" (namespaced key), "asset_id" (namespaced key),
    // "camera_overlay" (namespaced key), "dispensable" (boolean), "swappable" (boolean), "damage_on_hurt" (boolean),
    // "equip_on_interact" (boolean), "can_be_sheared" (boolean), "shear_sound" (namespaced key),
    // and optionally "allowed_entities" (a ListTag of entity types, or a single '#'-prefixed entity tag key, e.g. "#skeletons").
    // @mechanism
    // Provide no input to reset the item to its default value.
    // -->


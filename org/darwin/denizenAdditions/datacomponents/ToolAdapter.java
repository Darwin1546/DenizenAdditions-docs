    // <--[property]
    // @object ItemTag
    // @name tool
    // @input MapTag
    // @plugin DenizenAdditions
    // @description
    // Controls vanilla tool behavior. <@link language Item Components>.
    // Map keys are "default_mining_speed" (decimal), "damage_per_block" (integer),
    // "can_destroy_blocks_in_creative" (boolean), and optional "rules" (ListTag of MapTags).
    // Rule maps use "blocks" (a ListTag of block materials/keys, or a single '#'-prefixed block tag key, e.g. "#wool"),
    // plus optional "speed" (decimal) and "correct_for_drops" (boolean or "not_set").
    // @mechanism
    // Provide no input to reset the item to its default value.
    // -->

